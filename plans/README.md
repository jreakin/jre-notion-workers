# plans/

Every plan lives in `plans/{NNNN}-{slug}/` with `PLAN.md` and `STATUS.md` (`draft` | `in-progress` | `complete` | `abandoned`).

Numbers are sequential and never reused, same as `docs/adr/`. No plan file lives directly in this directory except this README.

See `AGENT-DOCS-VERSIONING.md`.
