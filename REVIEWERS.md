# REVIEWERS.md — which subagent reviews what

Auto-generated by project-alignment (Step 5) from this repo's detected stack and the subagents actually present in `.claude/agents/`. Regenerate via `/project-alignment` rather than hand-editing — the canonical roster lives in the project-alignment skill's `references/reviewers-routing.md`.

Reviewer roster: 11 total (from `references/reviewers-routing.md`), 4 installed in `.claude/agents/` this repo, 7 correctly listed as Not installed.

## Installed reviewers

| Touched | Subagent | Enforcement | Scope |
|---|---|---|---|
| Any diff, any language | `code-reviewer` | On-demand — also the receipt `4.7.16 pre-pr-review-gate.sh` blocks PR creation without | Diff vs. AGENTS.md Definition of Done + NEVER DO list |
| Auth, route handlers, env var handling | `security-auditor` | On-demand (PROACTIVE convention — invoke after touching these areas) | OWASP Top 10, hardcoded secrets, PII exposure |
| Before declaring any multi-step task complete | `task-critic` | On-demand (PROACTIVE convention) | Completeness/wiring audit — half-done work, silently skipped items, unwired code. Not code quality (that's `code-reviewer`) |
| Once, at the alignment's own Step 7 closing sequence — strictly after `task-critic`'s PASS receipt | `post-alignment-code-conformance-reviewer` | Automatic (dispatched by project-alignment itself) — findings feed the shared `gate.py` disposition ledger via `record-failure`, no dedicated Stop hook | Pre-existing code vs. conventions the retrofit just wrote into AGENTS.md/GUARDRAILS.md/ARCHITECTURE.md |

## Not installed in this repo

The following review-capable subagents exist in the Abstract Data roster but aren't installed here because this repo doesn't match their trigger condition. Re-run `/project-alignment` after adopting the relevant stack to install them.

- `tanstack-change-reviewer` — installs when `TANSTACK_DETECTED=1`
- `terraform-change-reviewer` — installs when `*.tf`/`*.tfvars` files are detected
- `db-migrator` — installs when `project_type=api`
- `supabase-auditor` — installs when `project_type=api` with Supabase in stack
- `api-contract-checker` — installs when `project_type=api`
- `pipeline-auditor` — installs when `project_type=pipeline`
- `data-validator` — installs when `project_type=pipeline`

## Last generated

2026-08-22 by project-alignment v2.21.1
