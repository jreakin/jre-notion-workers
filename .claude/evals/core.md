# Eval: core

## Capabilities

- "Worker module registers and exits 0" — `bun run dev` in `jre-notion-workers/` — target: pass@3 > 90%
- "Status-line eval set" — `tests/evals/status-lines.eval.ts`

## Regressions

- (none yet — add as features land)
