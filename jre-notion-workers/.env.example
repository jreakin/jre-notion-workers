# Copy this file to .env and fill in real values.
# Never commit .env — it is gitignored.
# 1Password users: run bash scripts/load-secrets.sh instead.

# Production credentials
NOTION_TOKEN=
DOCS_DATABASE_ID=
HOME_DOCS_DATABASE_ID=
TASKS_DATABASE_ID=
SYSTEM_CONTROL_PLANE_PAGE_ID=
DEAD_LETTERS_DATABASE_ID=

# Testing (integration tests only — never use production values here)
TEST_NOTION_TOKEN=
TEST_DOCS_DATABASE_ID=
